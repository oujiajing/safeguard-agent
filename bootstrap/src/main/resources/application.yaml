server:
  port: 9090
  servlet:
    context-path: /api/safeguard-agent

spring:
  application:
    name: safeguard-agent${unique-name:}-service

  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 100MB

  datasource:
    driver-class-name: org.postgresql.Driver
    type: com.zaxxer.hikari.HikariDataSource
    username: ${SAFEGUARD_POSTGRES_USER:postgres}
    password: ${SAFEGUARD_POSTGRES_PASSWORD:?set SAFEGUARD_POSTGRES_PASSWORD}
    url: ${SAFEGUARD_POSTGRES_URL:jdbc:postgresql://127.0.0.1:15432/safeguard?client_encoding=UTF8}
    hikari:
      connection-timeout: 5000
      idle-timeout: 600000
      max-lifetime: 1800000
      maximum-pool-size: 10
      minimum-idle: 5
      pool-name: SafeGuardHikariPool

  data:
    redis:
      host: ${SAFEGUARD_REDIS_HOST:127.0.0.1}
      port: ${SAFEGUARD_REDIS_PORT:6379}
      password: ${SAFEGUARD_REDIS_PASSWORD:?set SAFEGUARD_REDIS_PASSWORD}

safeguard:
  engine:
    type: agent  # 执行架构档位，可选 workflow（v1 编排管线）/ agent（v2 ReAct 架构，RAG 管线降级为主 Agent 的一个 Tool）
  demo-mode: ${SAFEGUARD_DEMO_MODE:false}
  eval:
    enabled: true

  vlm:
    base-url: ${SAFEGUARD_VLM_BASE_URL:http://localhost:11434}
    model: ${SAFEGUARD_VLM_MODEL:qwen3-vl:8b-instruct-q4_K_M}
    max-image-bytes: 8388608
    max-candidates: 8
  service:
    token: ${SAFEGUARD_SERVICE_TOKEN:}
  safe-team:
    enabled: ${SAFE_TEAM_ENABLED:false}
    natural-language-write-enabled: ${SAFE_TEAM_NATURAL_LANGUAGE_WRITE_ENABLED:false}
    base-url: ${SAFE_TEAM_BASE_URL:http://localhost:8080}
    dev-token: ${SAFE_TEAM_DEV_TOKEN:}
    service-token: ${SAFEGUARD_SERVICE_TOKEN:}
    connect-timeout-ms: ${SAFE_TEAM_CONNECT_TIMEOUT_MS:2000}
    request-timeout-ms: ${SAFE_TEAM_REQUEST_TIMEOUT_MS:8000}
    read-max-retries: ${SAFE_TEAM_READ_MAX_RETRIES:1}

rocketmq:
  name-server: ${SAFEGUARD_ROCKETMQ_NAME_SERVER:127.0.0.1:9876}
  producer:
    group: safeguard-producer${unique-name:}_pg
    send-message-timeout: 2000

# Agent 执行架构（safeguard.engine.type=agent 时生效）；单模型无 fallback，供应商可用性由 provider 侧保障
agent:
  chat:
    provider: ollama   # 文字与图像统一使用本地 qwen3-vl
    model: qwen3-vl:8b-instruct-q4_K_M
  max-iters: 10         # ReAct 循环上限，超出后熔断收尾
  max-retries: 2        # 单次模型调用失败重试次数
  sse-timeout-ms: 900000
  memory:
    # qwen3-vl 本地运行时默认上下文约 4096 token，按中文约 4 字符/token 预留系统提示、工具 schema 和输出空间
    context-window-chars: 24000
    summary-enabled: true
    long-term-enabled: true
    evictable-tools: [ search_knowledge ]

rag:
  legal:
    chunk:
      max-tokens: 450
      hard-limit-tokens: 600

  storage:
    type: s3  # 可选 s3（rustfs / minio）/ oss（阿里云）
    kb-bucket: safeguard-sources        # 全局知识库桶（私有），所有知识库文档按 collectionName 目录隔离；OSS 桶名全局唯一，生产需按部署覆盖
    asset-bucket: safeguard-assets # 多模态资产桶（公共读），PDF 抽出的图片等供浏览器匿名直连预览
    s3:
      endpoint: http://localhost:9000
      access-key: ${SAFEGUARD_RUSTFS_ACCESS_KEY_ID:?set SAFEGUARD_RUSTFS_ACCESS_KEY_ID}
      secret-key: ${SAFEGUARD_RUSTFS_SECRET_ACCESS_KEY:?set SAFEGUARD_RUSTFS_SECRET_ACCESS_KEY}
      region: us-east-1
      path-style: true
      public-url:  # 内外网端点不同时配置，留空回退 endpoint
    oss:
      endpoint: https://oss-cn-hangzhou.aliyuncs.com
      access-key: ${OSS_ACCESS_KEY:}
      secret-key: ${OSS_SECRET_KEY:}
      region: cn-hangzhou
      public-url:  # 虚拟主机式基址（含 bucket 子域），如 https://safeguard-assets.oss-cn-hangzhou.aliyuncs.com；留空由 endpoint 推导

  vector:
    type: pg  # 可选 milvus / pg

  keyword:
    type: none  # 可选 none / es
    es:
      uris: http://127.0.0.1:9200
      index: rag_keyword_store  # 全库共享索引，按 collection_name 字段区分知识库
      analyzer: ik_max_word
      search-analyzer: ik_smart

  default:
    collection-name: rag_default_store
    dimension: 1536
    metric-type: COSINE
    sse-timeout-ms: 300000  # SSE 全局超时（毫秒），默认 5 分钟

  query-rewrite:
    enabled: true

  rerank:
    enabled: true

  citation:
    enabled: true  # 回答行内引用角标 [N](#cite-N)：开启会动态追加引用规则并为资料注入编号，增加首字延迟；关闭不影响文档级来源面板

  rate-limit:
    global:
      enabled: true
      max-concurrent: 10
      max-wait-seconds: 15
      lease-seconds: 30
      poll-interval-ms: 200

  memory:
    history-keep-turns: 8
    summary-enabled: true
    summary-start-turns: 9
    summary-max-chars: 400
    title-max-length: 30

  semaphore:
    document-upload:
      name: rag:document:upload
      max-concurrent: 10
      max-wait-seconds: 5
      lease-seconds: 300

  knowledge:
    schedule:
      scan-delay-ms: 10000
      lock-seconds: 900
      batch-size: 20
      min-interval-seconds: 60

  mcp:
    servers:
      - name: default
        url: http://localhost:9099

  search:
    # 检索漏斗三段预算，各自独立、单调收窄，启动时校验 recall-budget ≥ default-top-k 且 rerank-candidate-limit ≥ default-top-k
    default-top-k: 5            # ③ Phase 2B Legal MVP 最终返回条数
    recall-budget: 20           # ① 每通道召回条数（绝对值，向量意图路/关键词/图谱共用）；须 ≥ default-top-k，0=回退 default-top-k
    retrieval:
      candidate-top-k: 20       # Phase 2D evaluation candidate pool: 20 or 50
    rerank:
      final-top-k: 5             # Phase 2D evaluation final evidence count: 5 or 10
    # 检索作用域：本次请求该看哪些知识库，与用什么模态检索无关，故与 channels 平级、三条通道共读
    # 判定只看 KB 意图最高分，不看意图个数；关掉任一通道都不影响其余通道的作用域
    scope:
      min-intent-score: 0.4        # 低于此分的意图节点不参与「是否收窄」的判定
      confidence-threshold: 0.6    # KB 意图最高分低于此值 → 退化为全库检索
      supplement-ratio: 0.25       # 定向时各通道从自身产出额度划给「未命中库」的保底比例，兜住意图判错；0=关闭补充路，命中库已覆盖全库时自动不补
    channels:
      timeout-ms: 15000     # 单通道查询超时上限（毫秒）
      vector:
        enabled: true       # 向量通道总开关
      keyword:
        enabled: false      # 检索范围读引擎解析好的作用域，与向量通道同源
      web-search:
        enabled: false
        count: 5
        timeout-seconds: 10
        api-key: ${YDC_API_KEY:}
    fusion:
      strategy: rrf
      rrf-k: 20                   # 候选池通常仅 20~40 条，k=60 会过度平滑名次；调低至 20 让头部更有区分度（配合「检索归因」日志校准）
      rerank-candidate-limit: 20  # ② Phase 2B Legal MVP RRF Top20 -> Rerank
      channel-weights:
        vector: 1.0
        keyword: 1.0
        web-search: 0.5
    # 证据相关性闸门：检索只保证返回最像的 N 条，库里没答案时照样满额返回，这里补一道相关度下限
    evidence:
      min-rerank-score: 0.2   # 整批最高精排分低于此值则整批丢弃，0=关闭；随 reranker 而变，换模型看「检索归因 - 精排分布」重测

  trace:
    enabled: true
    max-error-length: 1000

  # 图片解析（图生文）配置
  # 产出一段自包含的知识文本：既说清"图是什么"，又完整保留所有文字与层级
  image-parse:
    description-prompt: |-
      请把这张图片的内容转写成一段自包含的知识文本，用于知识库检索与问答，不要描述颜色、布局、字体等视觉样式。要求：
      1. 开头用一两句话说明这张图是什么（主题、类型、用途），让人不看图也能明白讲的是什么。
      2. 随后按图中的分组、层级、流程关系，完整转写所有文字内容（OCR），保留每个分组标题与其下属条目的归属关系，不要遗漏任何文字。
      3. 若是图表、架构图或流程图，请补充说明各部分之间的关系（如包含、调用、先后顺序），而不只是罗列文字。
      4. 保持术语准确完整，表述贴近用户可能的提问方式。
      5. 最后另起一行，用一句话自然引出图片（例如"下方为该内容对应的原始图片："），以便在这段文字后紧接着展示图片。
    max-output-tokens: 4096

ai:
  providers:
    ollama:
      url: http://localhost:11434
      endpoints:
        chat: /v1/chat/completions
        embedding: /v1/embeddings
    tei:
      url: http://127.0.0.1:18083
      endpoints:
        embedding: /v1/embeddings
    bailian:
      url: https://dashscope.aliyuncs.com
      api-key: ${BAILIAN_API_KEY:}
      endpoints:
        chat: /compatible-mode/v1/chat/completions
        rerank: /api/v1/services/rerank/text-rerank/text-rerank
        embedding: /compatible-mode/v1/embeddings
    aihubmix:
      url: https://aihubmix.com
      api-key: ${AIHUBMIX_API_KEY:}
      endpoints:
        chat: /v1/chat/completions
        embedding: /v1/embeddings
    siliconflow:
      url: https://api.siliconflow.cn
      api-key: ${SILICONFLOW_API_KEY:}
      endpoints:
        chat: /v1/chat/completions
        embedding: /v1/embeddings

  selection:
    failure-threshold: 2
    open-duration-ms: 30000

  stream:
    message-chunk-size: 1

  chat:
    candidates:
      - id: qwen-plus
        provider: bailian
        model: qwen-plus-latest
      - id: qwen-flash
        provider: bailian
        model: qwen-flash
      - id: qwen3-local
        provider: ollama
        model: qwen3-vl:8b-instruct-q4_K_M
        supports-thinking: true
      - id: qwen3-max
        provider: bailian
        model: qwen3-max
        supports-thinking: true
      - id: gpt-5.4
        provider: aihubmix
        model: gpt-5.4
      - id: deepseekv4pro
        provider: siliconflow
        model: deepseek-ai/DeepSeek-V4-Pro
        supports-thinking: true

    default-tier: standard
    deep-thinking-tier: deep

    tiers:
      fast:
        candidates: [ qwen3-local ]
        timeout-ms: 5000
      standard:
        candidates: [ qwen3-local ]
        timeout-ms: 120000
      deep:
        candidates: [ qwen3-local ]
        timeout-ms: 180000

  embedding:
    default-model: bge-m3
    candidates:
      - id: bge-m3
        provider: tei
        model: bge-m3
        dimension: ${rag.default.dimension}
        priority: 1
      - id: qwen-emb-8b
        provider: bailian
        model: text-embedding-v4
        dimension: ${rag.default.dimension}
        priority: 1
      - id: qwen-emb-local
        provider: ollama
        model: qwen3-embedding:8b-fp16
        dimension: ${rag.default.dimension}
        priority: 2
      - id: text-embedding-3-large
        provider: aihubmix
        model: text-embedding-3-large
        dimension: ${rag.default.dimension}
        priority: 3

  rerank:
    default-model: qwen3-rerank
    candidates:
      - id: qwen3-rerank
        provider: bailian
        model: qwen3-rerank
        priority: 1
      - id: rerank-noop
        provider: noop
        model: noop
        priority: 100

  vlm:
    default-model: qwen-vl-max
    candidates:
      - id: qwen-vl-max
        provider: bailian
        model: qwen-vl-max

# MinerU SaaS API 配置（PDF / Word / PPT 走 MinerU 解析）
mineru:
  api-url: https://mineru.net/api/v4
  api-key: ${MINERU_API_KEY:}
  poll-interval-seconds: 5
  timeout-seconds: 300
  enable-table: true
  enable-formula: true
  ocr: false
  language: ch
  concurrency-limit: 5
  semaphore-name: rag:mineru:parse
  max-wait-seconds: 30
  lease-seconds: 900

rustfs:
  url: ${SAFEGUARD_RUSTFS_URL:http://localhost:9000}
  access-key-id: ${SAFEGUARD_RUSTFS_ACCESS_KEY_ID:?set SAFEGUARD_RUSTFS_ACCESS_KEY_ID}
  secret-access-key: ${SAFEGUARD_RUSTFS_SECRET_ACCESS_KEY:?set SAFEGUARD_RUSTFS_SECRET_ACCESS_KEY}

sa-token:
  token-name: Authorization
  timeout: 2592000
  is-concurrent: true
  is-share: false
  token-style: simple-uuid
  is-log: false
  is-print: false

milvus:
  uri: http://localhost:19530
