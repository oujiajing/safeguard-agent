package com.safeguard.agent.legal.diagnostics;

public enum LegalDuplicateType {
    EXACT_DUPLICATE,
    NEAR_DUPLICATE,
    SAME_NUMBER_DIFFERENT_ROLE,
    SOURCE_EXACT_DUPLICATE,
    SOURCE_NEAR_DUPLICATE,
    PARSER_DUPLICATE,
    STRUCTURAL_VALID,
    UNKNOWN
}
